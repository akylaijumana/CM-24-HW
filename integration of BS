#include <iostream>
#include <cmath>
using namespace std;

double integration(double x) {
    return x; 
}

int main() {
    double a = 0.0; 
    double b = 1.0;  
    int n = 100;    
    double I = 0.0; 
    int K = 0;       

    srand(time(0));  

    while (K < n) {
        double r = (b - a) * rand(a-b) + a;  
        I += integration(r) * (b - a);  
        K++;
    }
    cout << I / K << endl; 
    return 0;
}
